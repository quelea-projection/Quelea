To launch Quelea, run Quelea.jar by either double clicking on the file or running:

java -jar Quelea.jar

from a shell. You will need the JRE (at least version 6) installed for this software to work, visit http://www.oracle.com/technetwork/java/javase/downloads/index.html to download if it is not already installed.